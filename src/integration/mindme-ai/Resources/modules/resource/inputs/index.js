/**
 * Generic "resource inputs" module.
 *
 * Any resource can declare other resources as its inputs through the
 * ResourceReference API (mindme-ai integration bundle).
 */

import {ResourceInputsEditor} from '#/integration/mindme-ai/resource/inputs/components/editor'
import {ResourceInputs} from '#/integration/mindme-ai/resource/inputs/components/display'

export {
  ResourceInputsEditor,
  ResourceInputs
}
