/*
 * Dashboard recommendations widget entry point (recommended products).
 */

import {RecommendationsBlock} from './components/main'

export const RecommendationsWidget = {
  name: 'dashboard-recommendations',
  component: RecommendationsBlock,
}

export default RecommendationsWidget