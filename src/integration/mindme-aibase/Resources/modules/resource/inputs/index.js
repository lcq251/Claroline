/**
 * Generic "resource inputs" module (mindme-aibase bundle).
 *
 * Any resource can declare other resources as its inputs through the
 * ResourceReference API (mindme-aibase integration bundle).
 */

import {ResourceInputsEditor} from '#/integration/mindme-aibase/resource/inputs/components/editor'
import {ResourceInputs} from '#/integration/mindme-aibase/resource/inputs/components/display'

export {
  ResourceInputsEditor,
  ResourceInputs
}