import {PropTypes as T} from 'prop-types'

const Template = {
  propTypes: {
    id: T.string,
    name: T.string,
    type: T.string,
    content: T.string,
    lang: T.string
  }
}

export {
  Template
}
